local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "🔰Liam`s Hub🔰",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "Script Loading...",
   LoadingSubtitle = "Getting Your Settings Ready",
   Theme = "Default", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "Big Hub"
   },

   Discord = {
      Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },

   KeySystem = true, -- Set this to true to use our key system
   KeySettings = {
      Title = "Liam`s Hub",
      Subtitle = "Key System",
      Note = "Only @liam_dc.dev2 Knowing the Key", -- Use this to tell the user how to get a key
      FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"liam."} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})

local MainTab = Window:CreateTab("Homepage", nil) -- Title, Image
local MainSection = MainTab:CreateSection("Main")

local Label = MainTab:CreateLabel("Emergency Emden: XVAEGD", nil, Color3.fromRGB(40, 40, 255), true) -- Title, Icon, Color, IgnoreTheme
local Label = MainTab:CreateLabel("Made by, @liam_dc.dev2", nil, Color3.fromRGB(40, 40, 255), true) -- Title, Icon, Color, IgnoreTheme

local CheatTab = Window:CreateTab("Cheat", nil) -- Title, Image
local CheatSection = CheatTab:CreateSection("Cheats")

local Paragraph = MainTab:CreateParagraph({Title = "Wichtig!", Content = "Benutzung ohne erlaubnis des Cheats fürt dazu das ein anderer Key gemacht wird."})

local Button = CheatTab:CreateButton({
   Name = "Aktivate Fly GUI V3",
   Callback = function()
   print("Made by Liam")
   loadstring(game:HttpGet("https://raw.githubusercontent.com/XNEOFF/FlyGuiV3/main/FlyGuiV3.txt"))()
   end,
})

local Button = CheatTab:CreateButton({
   Name = "Spam Console A.H on Top 15x",
   Callback = function()
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   print("Adolf Hitler On Top")
   end,
})

